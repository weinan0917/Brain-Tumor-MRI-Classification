# Brain Tumor MRI Project Dependencies
# Install (recommended, Tsinghua mirror):
#   python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#
# Install PyTorch separately if the default wheel does not match your CUDA/CPU setup:
#   https://pytorch.org/get-started/locally/

# ── Core numerics & data ─────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# ── Image I/O & processing ───────────────────────────────────
Pillow>=10.0.0
scikit-image>=0.22.0
imagehash>=4.3.1

# ── Machine learning (TensorFlow / Keras models) ─────────────
scikit-learn>=1.3.0
tensorflow>=2.15.0

# ── PyTorch models (ViT-B, ConvNeXtTiny) ────────────────────
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# ── Config & utilities ───────────────────────────────────────
PyYAML>=6.0
tqdm>=4.65.0

# ── MATLAB .mat (BrainTumorDataPublic in data_mix.py) ────────
h5py>=3.10.0

# ── Visualization ────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Explainability (ResNet50V2/XAI.py) ───────────────────────
lime>=0.2.0
shap>=0.43.0
